# Copy to `.env.dev` and `.env.prod`. Use a different BETTER_AUTH_SECRET per
# stage. Generate one with: openssl rand -base64 32

BETTER_AUTH_SECRET=
AUTH_TRUSTED_ORIGINS=
ELECTRON_PROTOCOL=com.tabaaq.desktop
MOBILE_PROTOCOL=com.tabaaq.mobile
