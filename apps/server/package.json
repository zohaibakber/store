{
  "name": "@store/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd ../.. && bun alchemy dev --stage dev --env-file .env.dev",
    "check": "tsc --noEmit",
    "test": "vitest run --root ../.. apps/server/test"
  },
  "dependencies": {
    "@effect/sql-sqlite-do": "catalog:",
    "@store/auth": "workspace:*",
    "@store/contracts": "workspace:*",
    "@store/db": "workspace:*",
    "@store/services": "workspace:*",
    "drizzle-orm": "catalog:",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/sql-libsql": "catalog:",
    "@effect/vitest": "catalog:",
    "@libsql/client": "^0.17.4",
    "@types/node": "^26.2.0",
    "alchemy": "catalog:",
    "rolldown": "1.2.4",
    "typescript": "catalog:",
    "vitest": "4.1.10"
  }
}
