{
  "name": "@store/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote --port 8787",
    "deploy": "wrangler deploy",
    "typegen": "wrangler types --env-interface CloudflareBindings --env-file .wrangler-types.env",
    "check": "wrangler types --env-interface CloudflareBindings --env-file .wrangler-types.env --check && tsc --noEmit",
    "test": "vitest run --root ../.. apps/server/src",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@effect/sql-pg": "4.0.0-beta.98",
    "@store/auth": "workspace:*",
    "@store/contracts": "workspace:*",
    "@store/db": "workspace:*",
    "drizzle-orm": "1.0.0-rc.4",
    "effect": "4.0.0-beta.98",
    "hono": "^4.12.29"
  },
  "devDependencies": {
    "@effect/sql-pglite": "4.0.0-beta.98",
    "@effect/vitest": "4.0.0-beta.98",
    "@electric-sql/pglite": "^0.5.4",
    "@types/node": "^24.13.3",
    "typescript": "^6.0.3",
    "vitest": "^4.0.18",
    "wrangler": "^4.110.0"
  }
}
