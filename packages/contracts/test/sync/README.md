Compat until retirement: Durable Object / `/api/sync/live` WebSocket types.

These tests stay and still run in `vp test`. They cover the live-sync protocol
contracts, not the PowerSync catalog path. Keep them until that engine is
explicitly retired.
