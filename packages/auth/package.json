{
  "name": "@store/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/auth.ts"
  },
  "scripts": {
    "auth:generate": "better-auth generate --config src/auth.ts --output src/auth-schema.ts --yes",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@better-auth/electron": "^1.6.23",
    "@neondatabase/serverless": "^1.1.0",
    "better-auth": "^1.6.23",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@better-auth/cli": "1.4.21",
    "@types/node": "^24.13.3",
    "drizzle-kit": "^0.31.8",
    "typescript": "^6.0.3"
  }
}
