name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Set desktop app version from tag
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          node -e '
            const fs = require("node:fs");
            const path = "apps/desktop/package.json";
            const pkg = JSON.parse(fs.readFileSync(path, "utf8"));
            pkg.version = process.argv[1];
            fs.writeFileSync(path, JSON.stringify(pkg, null, 2) + "\n");
          ' "$VERSION"

      - name: Build and publish
        working-directory: apps/desktop
        run: bun run release
        env:
          # Publishes to the public store-electron-releases repo, which the
          # default GITHUB_TOKEN cannot write to.
          GH_TOKEN: ${{ secrets.GH_RELEASES_TOKEN }}
          VITE_API_URL: ${{ secrets.VITE_API_URL }}
          ELECTRON_PROTOCOL: ${{ secrets.ELECTRON_PROTOCOL }}
